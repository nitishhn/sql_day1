<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>All Employee Details Page</title>
  </head>
  <body>
    <h3>This is my first view page created using EJS View Engine</h3>
    <hr />

    <h2>All Employee Details Page</h2>
    <hr />

    <!-- Loop through employee data -->
    <table>
      <tr>
        <th>Employee Number</th>
        <th>Name</th>
        <th>Job</th>
        <th>Salary</th>
        <th>Department Number</th>
      </tr>
      <!-- Assuming empArr is an array of objects with properties: empno, ename, job, salary, deptno -->
      <% for (let item of empArr) { %>
      <tr>
        <td><%= item.empno %></td>
        <td><%= item.ename %></td>
        <td><%= item.job %></td>
        <td><%= item.salary %></td>
        <td><%= item.deptno %></td>
      </tr>
      <% } %>
      <div>
        <a href="public\about.html"></a>
      </div>
    </table>
  </body>
</html>
